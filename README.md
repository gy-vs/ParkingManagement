# 停车场管理系统

## How to Run

### Docker 运行

#### 前置要求
- Docker
- Docker Compose

#### 构建和运行

1. 在项目根目录下运行：
```bash
docker-compose up --build -d
```

2. 进入容器：
```bash
docker exec -it parking-lot-backend /bin/bash
```

3. 在容器内运行程序：
```bash
cd /app/build
./parking_lot
```

4. 停止容器：
```bash
docker-compose down
```

#### 跨平台验证

验证镜像是否可以在 ARM 环境下使用：
```bash
docker pull --platform linux/arm64 parking-lot-backend
```

### 本地运行

#### 前置要求
- C++ 编译器（支持 C++11）
- CMake 3.10 或更高版本

#### 编译和运行

1. 进入 backend 目录：
```bash
cd backend
```

2. 创建构建目录并编译：
```bash
mkdir build
cd build
cmake ..
make
```

3. 运行程序：
```bash
./parking_lot
```

#### 运行参数

```bash
./parking_lot [选项]
```

选项：
- `-c, --capacity <数量>` - 设置停车场容量（默认: 5）
- `-r, --rate <费率>` - 设置每小时费率（默认: 1）
- `-b, --batch` - 批处理模式
- `-h, --help` - 显示帮助信息

示例：
```bash
# 使用默认配置运行
./parking_lot

# 设置停车场容量为10，费率为2
./parking_lot -c 10 -r 2

# 批处理模式
./parking_lot -b
```

## Services

本项目为纯后端项目，包含以下服务：

- **parking-lot-backend**: 停车场管理系统核心服务
  - 基于栈和队列实现停车场管理逻辑
  - 支持交互模式和批处理模式
  - 提供完整的车辆到达、离开、费用计算功能

## 测试账号

本项目为命令行应用程序，无需登录账号。

## 题目内容

课题名称：停车场管理系统

主要目标：
设有一个可以停放n辆汽车的狭长停车场，它只有一个大门可以供车辆进出。车辆按到达停车场时间的早晚依次从停车场最里面向大门口处停放(最先到达的第一辆车放在停车场的最里面)。如果停车场已放满n辆车，则后来的车辆只能在停车场大门外的便道上等待，一旦停车场内有车开走，则排在便道上的第一辆车就进入停车场。停车场内如有某辆车要开走，在它之后进入停车场的车都必须先退出停车场为它让路，待其开出停车场后，这些辆再依原来的次序进场。每辆车在离开停车场时，都应根据它在停车场内停留的时间长短交费。如果停留在便道上的车未进停车场时，允许其离去，不收停车费，并且仍然保持在便道上等待的车辆的次序。编制程序模拟该停车场的管理。

具体要求：
要求程序输出每辆车到达后的停车位置(停车场或便道上)，以及某辆车离开停车场时应交纳的费用和它在停车场内停留的时间。

实现提示：
汽车的模拟输入信息格式可以是：(到达/离去, 汽车牌照号码, 到达/离去的时刻)。例如，(A, 1, 5)表示1号牌照车在5这个时刻到达，而(D, 5, 20)表示5号牌照车在20这个时刻离去。整个程序可以在输入信息为(E, 0, 0)时结束。本题可用栈和队列来实现。

## 项目结构

```
label-01170/
├── backend/
│   ├── include/
│   │   ├── Stack.h          # 栈数据结构
│   │   ├── Queue.h          # 队列数据结构
│   │   ├── Car.h            # 车辆类
│   │   └── ParkingLot.h     # 停车场管理类
│   ├── src/
│   │   └── main.cpp         # 主程序
│   ├── CMakeLists.txt       # CMake 配置文件
│   └── Dockerfile           # Docker 镜像构建文件
├── docker-compose.yml       # Docker Compose 配置文件
├── .gitignore              # Git 忽略文件
└── README.md               # 项目说明文档
```

## 功能特性

1. **车辆到达处理**
   - 停车场有空位时，车辆进入停车场
   - 停车场已满时，车辆进入便道等待
   - 显示车辆到达后的停车位置

2. **车辆离开处理**
   - 车辆离开停车场时，计算停车时间和费用
   - 自动处理让路逻辑
   - 便道上的第一辆车自动进入停车场

3. **便道管理**
   - 支持便道上的车辆直接离开（不收费）
   - 保持便道车辆次序不变

4. **状态显示**
   - 实时显示停车场状态
   - 显示停车场内车辆信息
   - 显示便道等待车辆信息
   - 显示总收入统计

5. **两种运行模式**
   - 交互模式：支持多种命令交互
   - 批处理模式：支持批量处理车辆进出

## 使用示例

### 交互模式示例

**注意**：输入时可以使用以下任意格式：

| 格式类型 | 示例 |
|---------|------|
| 英文括号 + 英文逗号 | `(A, 1, 5)` |
| 中文括号 + 中文逗号 | `（A，1，5）` |
| 英文括号 + 中文逗号 | `(A，1，5)` |
| 中文括号 + 英文逗号 | `（A, 1, 5)` |
| 无括号 + 英文逗号 | `A,1,5` |
| 无括号 + 中文逗号 | `A，1，5` |
| 无括号 + 空格分隔 | `A 1 5` |

```
> (A, 1, 5)
[到达] 车牌号 1 在时刻 5 到达，进入停车场位置 1

> (A，2，8)
[到达] 车牌号 2 在时刻 8 到达，进入停车场位置 2

> A,3,10
[到达] 车牌号 3 在时刻 10 到达，进入停车场位置 3

> (D, 2, 20)
[离开] 车牌号 2 在时刻 20 离开
       停留时间: 12 个时间单位
       应交费用: 12 元

> S
========== 停车场状态 ==========
停车场容量: 5
已停车辆: 2
可用车位: 3
等待车辆: 0
总收入: 12 元

停车场内车辆:
  位置 1: 车牌号 1, 到达时间 5
  位置 2: 车牌号 3, 到达时间 10
================================
```

### 批处理模式示例

输入：
```
(A, 1, 5)
(A, 2, 8)
(A, 3, 10)
(A, 4, 12)
(A, 5, 15)
(A, 6, 18)
(D, 3, 25)
(D, 1, 30)
(E, 0, 0)
```

输出：
```
[到达] 车牌号 1 在时刻 5 到达，进入停车场位置 1
[到达] 车牌号 2 在时刻 8 到达，进入停车场位置 2
[到达] 车牌号 3 在时刻 10 到达，进入停车场位置 3
[到达] 车牌号 4 在时刻 12 到达，进入停车场位置 4
[到达] 车牌号 5 在时刻 15 到达，进入停车场位置 5
[到达] 车牌号 6 在时刻 18 到达，停车场已满，在便道位置 1 等待
[离开] 车牌号 3 在时刻 25 离开
       停留时间: 15 个时间单位
       应交费用: 15 元
[进场] 车牌号 6 从便道进入停车场位置 5
[离开] 车牌号 1 在时刻 30 离开
       停留时间: 25 个时间单位
       应交费用: 25 元
批处理结束
```

## 技术实现

- **数据结构**：使用栈（Stack）管理停车场，使用队列（Queue）管理便道
- **算法**：栈的弹出和压入操作实现车辆让路逻辑
- **费用计算**：基于停车时长和费率计算停车费用
- **错误处理**：完善的输入验证和错误提示

## 扩展性

项目采用模块化设计，核心逻辑清晰，便于扩展：
- 可轻松修改停车场容量和费率
- 可添加更多车辆管理功能
- 可扩展为图形界面或 Web 应用
